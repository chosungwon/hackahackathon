import React, { Component } from "react";
// import io from 'socket.io-client';

class Chat extends Component{
    render(){
        return(
            <input type={'text'}/>
        )
    }
}

export default Chat;